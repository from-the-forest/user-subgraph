"""
User type
"""
type User {
  "User's first name"
  firstName: String!
  "Node ID"
  id: ID!
  "User's last name"
  lastName: String!
}

"""
Root query
"""
type Query {
  "The currently logged in user"
  whoami: User!
}
